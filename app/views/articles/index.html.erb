<div class="content-wrapper">
  <h1>My Writings</h1>
  <div class="content">
    <ul class="articles">
    <% @articles.each do |article| %>
      <li>
        <%= link_to article.title, article_path(article.slug), class: "underlined" %> 
        <time class="code"><%= article.published_at.strftime('%B %e, %Y') %></time>
      </li>
    <% end %>
    </ul>
  </div>
</div>